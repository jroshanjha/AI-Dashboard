version: '3'

services:
  fastapi:
    build: .
    ports:
      - "8000:8000"

  streamlit:
    image: python:3.10
    volumes:
      - .:/app
    working_dir: /app/frontend
    command: streamlit run streamlit_app.py
    ports:
      - "8501:8501"